server:
  port: ${PORT:8090}

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}
      
logging:
  level:
    com.example.qrgenerator: DEBUG
    org.springframework.data.mongodb: DEBUG
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG